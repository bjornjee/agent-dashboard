{
  "name": "agent-dashboard-adapter",
  "version": "0.34.1",
  "private": true,
  "description": "Claude Code adapter for agent-dashboard",
  "scripts": {
    "test": "node --test packages/*/test.js scripts/hooks/*.test.js scripts/*.test.js"
  }
}
