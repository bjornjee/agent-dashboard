{
  "name": "agent-dashboard-adapter",
  "version": "0.4.5",
  "private": true,
  "description": "Claude Code adapter for agent-dashboard",
  "scripts": {
    "test": "node --test packages/*/test.js scripts/hooks/*.test.js"
  }
}
